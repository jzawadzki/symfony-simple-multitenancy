{% extends 'base.html.twig' %}

{% block title %}Tenant{% endblock %}

{% block body %}
    <h1>Tenant</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ tenant.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ tenant.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('tenant_index') }}">back to list</a>

    <a href="{{ path('tenant_edit', {'id': tenant.id}) }}">edit</a>

    {{ include('tenant/_delete_form.html.twig') }}
{% endblock %}
